max_count := 100

width := 1920
height := 1080
# width * height * 3
image := [0; 6220800]

start_re := -2.1
end_re := 0.5

start_im := 1.2
end_im := -1.2

(
    max_count: max_count,
    width: width,
    height: height,
    image: image,
    start_re: start_re,
    end_re: end_re,
    start_im: start_im,
    end_im: end_im
)
